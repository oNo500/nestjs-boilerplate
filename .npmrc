engine-strict=true
enable-pre-post-scripts=true

# Hoist eslint-related packages so oxlint jsPlugins can resolve them at runtime.
# Required because pnpm strict mode isolates transitive deps, but oxlint
# resolves jsPlugin packages from the project root at runtime.
shamefully-hoist=true
